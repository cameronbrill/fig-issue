import type { NextPage } from "next";
import { HomePage } from "../components/Pages/Home/Home";

const Home: NextPage = () => {
  return (
    <section>
      <HomePage />
    </section>
  );
};

export default Home;
